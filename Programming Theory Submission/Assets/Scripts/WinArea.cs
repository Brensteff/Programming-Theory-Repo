using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinArea : MonoBehaviour
{
    private bool gameOver = false;


    // Update is called once per frame
    void Update()
    {
        
    }

    /*bool isGameOver()
    {
        if ()// player makes contact with the win platform
        {
            gameOver = true;
            // Play particle effects and show text
            // Exit play mode/go back to main menu if there is one
        }
    }*/
}
